export enum ModuleNameEnum {
  Dasboard = "dashboard",
  User = "user",
}

export enum AccessControllActionEnum {
  View = "view",
  List = "list",
  Detail = "detail",
  Create = "create",
  Update = "update",
  Delete = "delete",
}
